import { HoverTraceDirective } from './hover-trace.directive';

describe('HoverTraceDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverTraceDirective();
    expect(directive).toBeTruthy();
  });
});
