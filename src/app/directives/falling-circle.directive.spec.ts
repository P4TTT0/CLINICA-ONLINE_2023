import { FallingCircleDirective } from './falling-circle.directive';

describe('FallingCircleDirective', () => {
  it('should create an instance', () => {
    const directive = new FallingCircleDirective();
    expect(directive).toBeTruthy();
  });
});
